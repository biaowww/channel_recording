# ── 构建产物 ──
[Bb]in/
[Oo]bj/
# 本地打包的自包含 exe（体积大，不入库；发布走 GitHub Releases）
dist/
publish/
*.user
.vs/

# ── 本地录音 / 抓帧产物（不入库，含隐私音频）──
/recording/
*.wav
*.mp3
*.m4a
*.aac

# ── OS ──
Thumbs.db
desktop.ini
.DS_Store
